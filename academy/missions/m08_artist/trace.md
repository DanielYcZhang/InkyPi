# Trace a Small Example (M-08)

Use this example:
- `border=10`, `dot_radius=6`, `spacing=18`, `dot_y = h - border - dot_radius * 2`

1) What is the `(x1, y1, x2, y2)` for the first dot?
2) What is the `(x1, y1, x2, y2)` for the second dot?
